// Used to create a new array , [] brackets enclose list of values.

//Values separated by commas(,)

//Array can store any value including objects or arrays itself.

//Array is a special type of Object (little different from the traditional object of Java script)

//Array.isArray(array name); checks whether the given data structure is array or not. Returns True/False.

//.Length property returns the length of the array.

//push()/pop() add or remove element from end.
//shift()/unshift() add or remove element from start.
//splice(index no. , no. of elements to remove) removes element from any index
//toString() converts values to string
//sort() sorts elements 
//valueOf gets array copy 

//Arrays also use refernce same as objects.
//Destructuring also works for arrays.